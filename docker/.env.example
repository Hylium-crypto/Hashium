# Environment variables for Hashium Docker
# Copy this to .env and customize

# RPC credentials (CHANGE THESE!)
RPC_USER=hashium
RPC_PASS=your_secure_password_here

# Optional: External IP for peer discovery
# EXTERNAL_IP=your.vps.ip.address
